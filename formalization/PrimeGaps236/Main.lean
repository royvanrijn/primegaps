import PrimeGaps236.Stadlmann.BoundaryCertificate
import PrimeGaps236.Tuple

/-!
# Gap 236 formalization spine

What is proved now:

* the exact active candidate metadata and known unrestricted values;
* the arithmetic reduction for the pending boundary enclosure;
* the explicit admissible 48-tuple and `DHL[48,2] -> H₁ ≤ 236` endgame.

The future shaped-support sieve theorem must construct `DHL48`; no axiom or
`sorry` hides that remaining analytic obligation here.
-/
